name: covcopcomp_math_fact
description: ...

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  align_positioned: ^3.0.0
  charts_flutter: ^0.12.0
  cloud_firestore: ^3.1.8
  cupertino_icons: ^1.0.2
  firebase_auth: ^3.3.7
  firebase_core: ^1.12.0
  flutter_spinkit: ^5.1.0
  flutter_svg: ^1.0.3
  image_picker: ^0.8.5
  package_info: ^2.0.2
  path_provider: ^2.0.9
  provider: ^6.0.2
  url_launcher: ^6.0.20
  avatar_glow: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mathfacts.json
    - assets/coloured_paper.svg
    - assets/new_document.svg
    - assets/placeholdercircle.png
  fonts: 
    - family: RobotoMono
      fonts:
      - asset: fonts/RobotoMono-Regular.ttf
